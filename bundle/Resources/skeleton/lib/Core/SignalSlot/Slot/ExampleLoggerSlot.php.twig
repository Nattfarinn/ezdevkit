<?php
/**
 * @copyright Copyright (C) eZ Systems AS. All rights reserved.
 * @license For full copyright and license information view LICENSE file distributed with this source code.
 */
namespace {{ lib_namespace }}\Core\SignalSlot\Slot;

use eZ\Publish\Core\SignalSlot\Signal;
use eZ\Publish\Core\SignalSlot\Slot;
use {{ lib_namespace }}\Core\SignalSlot\Signal\ExampleCreateSignal;

class ExampleLoggerSlot extends Slot
{
    /**
     * Receive the given $signal and react on it.
     *
     * @param \eZ\Publish\Core\SignalSlot\Signal $signal
     *
     * @return bool
     */
    public function receive(Signal $signal)
    {
        if (!($signal instanceof ExampleCreateSignal)) {
            return false;
        }

        // throw new \Exception("[{$signal->timestamp}] Example id: {$signal->exampleId} created.");

        return true;
    }
}
