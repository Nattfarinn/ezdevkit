<?php
/**
 * @copyright Copyright (C) eZ Systems AS. All rights reserved.
 * @license For full copyright and license information view LICENSE file distributed with this source code.
 */
namespace {{ bundle_namespace }}\Controller;

use {{ lib_namespace }}\API\Repository\{{ bundle_name }}ServiceInterface;
use {{ lib_namespace }}\Core\REST\Server\RestController;
use Symfony\Component\HttpFoundation\Request;

/**
 * {{ bundle_name }} Controller class.
 */
class {{ bundle_name }}Controller extends RestController
{
    /** @var \{{ lib_namespace }}\API\Repository\{{ bundle_name }}ServiceInterface */
    protected ${{ bundle_name|lower }}Service;

    /**
     * {{ bundle_name }}Controller constructor.
     *
     * @param \{{ lib_namespace }}\API\Repository\{{ bundle_name }}ServiceInterface ${{ bundle_name|lower }}Service
     */
    public function __construct({{ bundle_name }}ServiceInterface ${{ bundle_name|lower }}Service)
    {
        $this->{{ bundle_name|lower }}Service = ${{ bundle_name|lower }}Service;
    }
{% if example %}

    /**
     * Creates new example data.
     *
     * @param Request $request
     *
     * @return \{{ lib_namespace }}\API\Repository\ValueObject\ExampleStruct
     */
    public function createExample(Request $request)
    {
        $exampleCreateStruct = $this->parseRequestContentType($request);

        return $this->{{ bundle_name|lower }}Service->createExample($exampleCreateStruct);
    }
{% endif %}
}